apply plugin: 'java'
apply plugin: 'scala'

repositories {
    mavenCentral()
}

// needs at least Gradle V1.7
wrapper {
    gradleVersion = '2.9'
}

dependencies {
    compile 'org.scala-lang:scala-library:2.11.7'
}